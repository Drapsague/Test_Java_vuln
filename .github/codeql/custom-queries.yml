name: "Custom queries"
description: "Custom queries for CodeQL analysis"
disable-default-queries: true
queries:
  - uses: security-extended
  - uses: security-and-quality
  - name: Custom Query
    uses: ./.github/codeql/xpath-injection.ql
  
paths:
  - src
